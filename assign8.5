✅ Q: How do you redirect both stdout and stderr to the same file in UNIX?
💡 A:
You can redirect both standard output (stdout) and standard error (stderr) to a single file using:


command > output.txt 2>&1
